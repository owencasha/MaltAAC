package mt.edu.um.malteseaacapp.activities;

import android.support.v7.app.AppCompatActivity;

public class MakeLauncher extends AppCompatActivity {
    /* Dummy activity...
       Used to show launcher chooser by disabling and enabling the activity.
       Since the activity is declared as a launcher in the manifest:
           <category android:name="android.intent.category.DEFAULT" />
           <category android:name="android.intent.category.HOME" />
       whenever it is disabled and enabled, the launcher chooser is shown. */
}
